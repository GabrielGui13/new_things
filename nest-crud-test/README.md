## Prisma

```
yarn add -D prisma => for prisma environment and cli
yarn add @prisma/client => for prisma database connection
```

```
yarn prisma init => generate prisma folder + schema
yarn prisma migrate dev => run migrations
yarn prisma migrate dev --name init => create migration
```
